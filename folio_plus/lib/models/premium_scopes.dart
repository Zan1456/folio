class PremiumScopes {
  // everything
  static const all = "folio.plus.*";

  // idk where it will be but i need it
  // static const renameTeachers = "folio.plus.RENAME_TEACHERS";
  // static const goalPlanner = "folio.plus.GOAL_PLANNER";
  // static const changeAppIcon = "folio.plus.CHANGE_APP_ICON";

  // tier 1 (Kupak) (Folio+)
  static const maxTwoAccounts = "folio.plus.MAX_TWO_ACCOUNTS";
  static const earlyAccess = "folio.plus.EARLY_ACCESS";
  static const totalGradeCalculator = "folio.plus.TOTAL_GRADE_CALCULATOR";
  static const welcomeMessage = "folio.plus.WELCOME_MESSAGE";
  static const unlimitedSelfNotes = "folio.plus.UNLIMITED_SELF_NOTES";
  static const customGradeRarities = "folio.plus.CUSTOM_GRADE_RARITIES";
  static const gradeExporting = "folio.plus.GRADE_EXPORTING";
  // tier scope
  // static const tierCap = "folio.plus.tier.CAP";

  // tier 2 (Tinta) (Folio+ Gold)
  static const noAccountLimit = "folio.plus.NO_ACCOUNT_LIMIT";
  static const appIconChange = "folio.plus.APP_ICON_CHANGE";
  static const liveActivityColor = "folio.plus.LIVE_ACTIVITY_COLOR";
  static const customFont = "folio.plus.CUSTOM_FONT";
  static const timetableNotes = "folio.plus.TIMETABLE_NOTES";
  static const unlimitedGoalPlanner = "folio.plus.UNLIMITED_GOAL_PLANNER";
  static const calendarSync = "folio.plus.CALENDAR_SYNC";
  // tier scope
  // static const tierInk = "folio.plus.tier.INK";

  // tier 3 (Szivacs)
  // cancelled
  // tier scope
  static const tierSponge = "folio.plus.tier.SPONGE";

  // uncategorized

  // old scopes
  static const nickname = "folio.plus.NICKNAME";
  static const gradeStats = "folio.plus.GRADE_STATS";
  static const customColors = "folio.plus.CUSTOM_COLORS";
  static const customIcons = "folio.plus.CUSTOM_ICONS";
  static const renameSubjects = "folio.plus.RENAME_SUBJECTS";
  static const timetableWidget = "folio.plus.TIMETALBE_WIDGET";
  static const fsTimetable = "folio.plus.FS_TIMETABLE";

  // new new tier scopes
  static const tierBasic = "folio.plus.tier.BASIC";
  static const tierGold = "folio.plus.tier.GOLD";
}
