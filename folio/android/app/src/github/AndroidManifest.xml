<!-- GitHub / sideload flavor — adds permissions required for APK self-update.
     These are intentionally absent from src/main/ so the Play Store build
     never declares them, regardless of manifest merger behaviour. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application android:requestLegacyExternalStorage="true" />

    <!-- APK download & install -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <!-- File storage access for downloaded APKs -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

</manifest>
